// Shared app configuration (tracked). Optional local secrets:
#include? "Secrets.xcconfig"

// Supabase keys are merged into Info.plist via `INFOPLIST_KEY_*` in `project.pbxproj`
// (xcconfig-only INFOPLIST_KEY values were not merged into the bundle in Xcode 26).
